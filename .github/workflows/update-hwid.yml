name: 🔑 Auto-Update HWID with Expiration

on:
  repository_dispatch:
    types: [update-hwid]

permissions:
  contents: write

jobs:
  update-hwid:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🔍 Update HWID with Expiration
        run: |
          KEY="${{ github.event.client_payload.key }}"
          HWID="${{ github.event.client_payload.hwid }}"
          USER="${{ github.event.client_payload.user }}"
          EXPIRATION="${{ github.event.client_payload.expiration }}"
          
          echo "🔄 Updating key: $KEY"
          echo "📱 HWID: $HWID"
          echo "👤 User: $USER"
          echo "⏰ Expiration: $EXPIRATION"
          
          # Create new entry format: Key:HWID:User:Expiration
          NEW_ENTRY="$KEY:$HWID:$USER:$EXPIRATION"
          
          # Backup original file
          cp wl.txt wl.txt.backup
          
          # Remove old entry if exists and add new one
          grep -v "^$KEY:" wl.txt.backup > wl.txt.temp || true
          echo "$NEW_ENTRY" >> wl.txt.temp
          mv wl.txt.temp wl.txt
          
          echo "📊 Changes made:"
          diff -u wl.txt.backup wl.txt || true

      - name: 💾 Commit and push changes
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git add wl.txt
          git diff --staged --quiet || git commit -m "🔧 Update ${{ github.event.client_payload.key }} - Expires: ${{ github.event.client_payload.expiration }}"
          git push
